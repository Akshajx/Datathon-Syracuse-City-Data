# DataThon — Syracuse code violations & assessment (Python 3.10+)
# Single environment: Track A (notebook + regression) and Track B (GeoPandas / Folium grid pipeline).

# --- Core analysis & notebook ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0
ipykernel>=6.0

# --- Track B: LifeScientist spatial pipeline (optional; same venv) ---
geopandas>=0.14
folium>=0.15
shapely>=2.0
fiona>=1.9
